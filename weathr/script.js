const city = document.getElementsByClassName("bar");

function myFunction(){
    if(city.value === "debre tabo"){
        document.addEventListener("condition").innerText = <i class='bx bxs-sun'></i>;
    }
}